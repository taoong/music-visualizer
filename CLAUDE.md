# Music Visualizer

Real-time audio-reactive visualizer built with TypeScript, p5.js, and Tone.js. Supports file upload, sample track, and live microphone input. BPM detection runs client-side via onset/autocorrelation.

## Commands

- `npm run dev` — start Vite dev server (port 3000)
- `npm run build` — `tsc && vite build` (output to `dist/`)
- `npm run lint` / `npm run lint:fix` — ESLint
- `npm run test` — run Vitest tests
- `npm run test:watch` — run Vitest in watch mode
- `npm run typecheck` — `tsc --noEmit`
- `npm run cap:sync` — build the web app and sync it into the iOS native project (`ios/`)
- `npm run cap:open:ios` — open the iOS project in Xcode

## Architecture

### Source layout

```
src/
├── main.ts                    # p5 sketch entry point, render loop, audio pipeline orchestration
├── state/store.ts             # Singleton EventEmitter store (AppState, Config, AudioProcessingState)
├── audio/
│   ├── engine.ts              # Tone.js player management (freq mode + mic mode)
│   ├── fft.ts                 # FFT analysis: log-band amplitudes, octave amplitudes, spectral centroid
│   ├── processing.ts          # Auto-gain normalization, transient detection, delta (rate of change)
│   ├── pipeline.ts            # Decay, octave processing, band smoothing with attack/release
│   └── bpm.ts                 # BPM detection: client-side onset/autocorrelation
├── __tests__/
│   ├── setup.ts               # Global test setup: browser global stubs for Node environment
│   ├── mocks/
│   │   ├── p5.ts              # Mock factories: createMockContext, createMockP5Image, createMockP5
│   │   └── store.ts           # Mock factory: createMockStoreState
│   └── helpers/
│       └── callOrder.ts       # expectCallSequence — verifies mock call ordering
├── visualizations/
│   ├── index.ts               # Barrel exports
│   ├── helpers.ts             # getBandData(), getBandAverages() — shared audio state accessors
│   ├── userImage.ts           # User image state: load/clear/get/has, emits 'imageChange' event
│   ├── circle.ts              # Rotating spike circle (7 bands x 60 spikes), optional center image
│   ├── spectrum.ts            # Horizontal bar chart
│   ├── tunnel.ts              # Octave-based concentric rings with glow, optional center image
│   ├── balls.ts               # Physics-based bouncing balls (VizMode: tetris) with beat kick boost
│   ├── lasers.ts              # Concert laser light show with beat-synced beams and frequency-driven color
│   ├── text.ts                # Beat-synced text patterns (7 modes: zoom, diagonal, quad-mirror, crown, echo, reflect, kaleidoscope)
│   ├── highway.ts             # Audio-reactive highway drive; perspective road with lane markings; beat fires headlight flares; freq bands drive scenery and road color; Three.js WebGL overlay
│   ├── liquidmetal.ts         # (lazy) Liquid Metal: molten chrome icosphere; Three.js PBR with RoomEnvironment IBL; amplitude deforms mesh; GlitchPass on transients; sliders: Rotation Speed, Spin Chaos
│   ├── neon.ts                # (lazy) Neon Grid: synthwave terrain mesh; Three.js vertex displacement driven by 7 bands; UnrealBloomPass; sliders: Camera Rotation, Intensity, Camera Height
│   ├── imagegrid.ts           # (lazy) 16×16 mosaic of 3D tiles viewed top-down; tile height driven by column's freq band; beat → radiating height-wave; image upload → tile tops show image regions
│   ├── sculpture.ts           # (lazy) 8 flat panels in a circle showing image strips; beat-synced camera orbit; audio-reactive tilt/glow/spacing; Three.js WebGL overlay
│   ├── binary.ts              # Matrix-style cascading binary/ASCII characters; columns mapped to 7 freq bands; beat-synced wave ripple; green-tinted with transient white flash
│   ├── rippletank.ts          # Ripple Tank: 7 point-sources in a ring emit circular waves per freq band; wave superposition creates interference patterns; beats spawn expanding shockwave rings; beat surge speeds up water on hits; offscreen pixel buffer at 1/4 res (1/6 mobile) with precomputed distance tables; sliders: Beat Frequency, Water Speed, Beat Surge
│   ├── cymatics.ts            # Cymatics: Chladni plate simulation; ~3000 particles (1500 mobile) drift toward nodal lines of 7 standing wave modes driven by freq bands; beat-triggered scatter/reform; HSB coloring by dominant band
│   ├── attractor.ts           # Strange Attractor: Lorenz attractor chaos theory simulation; particles trace 3D butterfly-shaped paths; freq bands spawn colored groups; amplitude warps sigma/rho; beats jolt parameters; 3D→2D projection with orbiting camera; sliders: Chaos, Trail Length
│   ├── mandala.ts             # Mandala: 5 concentric rotating shapes (grid, hexagon, square, triangle, circle) from largest to smallest; each shape has independent rotation speed slider; beat hits temporarily spike all rotation speeds; HSB colors driven by freq band amplitudes; glow layers on shapes; sliders: Beat Frequency, Grid/Hex/Square/Triangle/Circle Speed
│   ├── stringart.ts           # String Art: mathematical "times table" / cardioid visualization; N pins evenly around a circle connected by lines from pin i to pin round(i*M)%N; 7 colored layers (one per freq band) each with a slightly offset multiplier; fractional M morphs continuously between geometric shapes (cardioid, trefoil, etc.); bass amplitude nudges multiplier; beats shift hue palette; 3-pass glow (outer/mid/core); sliders: Pins (20–200), Multiplier (2–20), Drift Speed
│   ├── constellation.ts       # Constellation: drifting star particles forming network connections; stars mapped to freq bands by screen position; beats push stars outward; connection lines glow with audio energy; sliders: Star Count, Connection Range, Drift Speed
│   ├── petals.ts              # Petal Bloom: 7 concentric rings of bezier-curve flower petals (one per freq band); each ring rotates independently (alternating CW/CCW, audio-driven speed); amplitude blooms petal size and brightness; beat pulse bursts all rings outward; HSB palette slowly drifts; sliders: Petal Count, Bloom Scale, Spin Speed
│   ├── waterfall.ts           # Waterfall: 3D scrolling spectrogram; ring buffer of recent spectrum snapshots rendered as stacked ribbons in oblique projection (newest in front, older receding up-and-back with perspective scaling + atmospheric haze); amplitude = ribbon height, plasma palette colors each bin via a horizontal canvas gradient; beat flash glazes the scene; sliders: Scroll Speed, Brightness, Palette Hue
│   ├── kaleido.ts             # Kaleidoscope: audio spectrum mapped to a continuous closed curve in polar coordinates with N-fold mirror symmetry; radius at each angle interpolated from 7 freq bands; N rainbow-tinted wedge fills + 3-layer glow outline; organic phase-harmonic smear breaks perfect symmetry; beat jumps hue + flashes; trail effect creates layered mandala patterns; sliders: Segments (2–12), Trail, Organic
│   ├── kaleidoscope.ts        # Kaleidoscope: 7 oscillating polar curves (one per freq band) drawn in a source wedge and mirrored 2×N times around the canvas; each curve oscillates at integer frequency (b+1) so it closes seamlessly at the outer edge; beat snaps hue palette and widens oscillations; HSB colors per band drift over time; central glow; sliders: Segments (3–12), Complexity (1–4 layers), Spin Speed
│   ├── weave.ts               # Weave: audio-reactive tapestry; N horizontal warp threads + N vertical weft threads cross the canvas in a grid; each thread mapped to a freq band (weft offset by ½ BAND_COUNT for hue contrast); threads vibrate sinusoidally (amplitude scales with band energy); 3-pass glow per thread; intersection nodes light up proportional to product of both crossing bands' amplitudes — only bright where both are simultaneously active; beat fires an expanding radial shockwave ring from canvas centre; hue shifts on each beat; sliders: Threads (4–32 per axis), Glow (stroke/halo size), Pulse (shockwave intensity)
│   ├── synthwave.ts           # Synthwave: retro 80s perspective grid; cyan horizontal lines scroll toward viewer via t² perspective bunching; magenta vertical fan lines converge to vanishing point; striped glowing sun (yellow→pink) sits on horizon; 7 freq bands drive audio-reactive mountain silhouette heights; beats pulse sun + flash screen pink; ADD blendMode neon glow on all elements; sliders: Speed (scroll rate), Horizon (horizon height 0.2–0.65), Glow (neon intensity)
│   ├── bloom.ts               # Bloom: generative neon branching growth; tips radiate from canvas centre, steered by Perlin noise; sub-bass spawns thick warm-red roots that fork hierarchically up through orange→yellow→green→teal→blue→violet at finest tips (one hue per freq band); 3-pass glow (outer/mid/core) rendered to offscreen buffer with configurable fade rate; beats trigger root burst; amplitude drives continuous forking; sliders: Density (fork rate), Lifespan (trail persistence), Spread (branch deviation angle)
│   ├── monolith.ts            # (lazy) Monolith: tall faceted crystal obelisk (stretched icosahedron, 80 facets grouped into 7 freq bands) sits at world origin; camera is the protagonist with choreographed beat-driven moves — continuous orbit baseline, crash-zoom punch on every beat, Hitchcock dolly-zoom every 16 beats, sustained-bass pull-back, occasional snap-cuts, top-down/worm's-eye phases every 32 beats, barrel rolls on hi-hat transients, frame-rate-independent decay; Three.js WebGL overlay with UnrealBloomPass
│   ├── hive.ts                # Hive: full-canvas flat-top hexagonal honeycomb grid; 7 concentric radial zones each mapped to one freq band (sub-bass at centre → brilliance at edge); hex brightness driven by band amplitude; beat-triggered expanding ripple ring sweeps outward and nudges global hue palette; bloom glow ellipses behind each active hex; sliders: Hex Size (grid density), Glow (bloom intensity), Ripple (beat ripple strength)
│   ├── marbling.ts            # Marbling: full-screen psychedelic plasma/marble-paper colour field; 3 base sinusoidal waves + 7 audio-reactive band waves superpose into a continuous value field; value → HSV hue → pixel colour via offscreen pixel buffer at ¼ res (⅛ mobile); beat triggers hue-phase jump and brightness flash; sliders: Hue Shift (palette rotation 0–1), Zoom (pattern density 0–1), Speed (animation rate 0–1)
│   ├── flowfield.ts           # Flow Field: Perlin-noise vector field guiding colored brushstroke ribbons (inspired by Tyler Hobbs' "Fidenza" 2021); 7 bands mapped to distinct hues (violet→blue→teal→green→yellow→orange→magenta); amplitude drives ribbon speed, stroke weight, and brightness; sub-bass bends field spatially; beats burst fresh ribbons from canvas edges; 3-pass ADD glow per ribbon; offscreen trail buffer; sliders: Turbulence (laminar→chaotic), Trail (fade rate), Width (stroke weight)
│   ├── lissajous.ts           # Lissajous: 7 oscilloscope-style parametric curves (one per freq band) using coprime integer ratios 1:1→5:6 (ellipse, figure-8, three-lobe, etc.); inspired by Jerobeam Fenderson's oscilloscope music (creativeapplications.net); amplitude drives curve size; phases drift at configurable speed, snap on beats; 3-pass phosphor glow (green→cyan→blue→violet→magenta→amber palette); offscreen trail buffer; sliders: Curves (1–7 visible), Glow (phosphor brightness), Drift (phase speed)
│   ├── truchet.ts             # Truchet Tile Maze: audio-reactive Truchet quarter-circle arc grid inspired by Manolo Gamboa Naon (Manoloide) "88 Allegories" series; 7 freq bands drive color zones (column bands) and tile orientation morph speed (violet→cyan→teal→lime→gold→orange→magenta palette); beat triggers mass orientation shuffle + hue palette jump; brilliance drives continuous sparkle perturbations; 2-pass glow (wide outer halo + bright core); sub-bass boosts stroke weight; sliders: Grid (tile density 0=large→1=small), Speed (morph rate), Glow (arc weight/halo)
│   ├── topography.ts          # Topography: full-canvas audio-reactive topographic contour map inspired by Tyler Hobbs' "Meridian" series (2022); 7 freq bands drive elevation of horizontal stripes (sub-bass=left → brilliance=right) via Gaussian weighting; Perlin noise base terrain drifts over time; marching squares extracts iso-contour lines at 3–15 elevation levels; each level hue-mapped to nearest freq band (blue=sub → red=brilliance); 2-pass neon glow (wide halo + bright core); beat radiates circular elevation surge from canvas centre; sliders: Resolution (grid density 10–60), Levels (contour count 3–15), Speed (animation rate)
│   ├── interference.ts        # Interference: audio-reactive moiré / interference-pattern visualizer inspired by Ryoji Ikeda's "test pattern" series; 7 freq bands each drive a standing-wave layer at evenly distributed angles (0°–154°); superposition creates shifting moiré fringes that tilt, breathe, and shimmer with the music; audio phase offsets + amplitude-driven angular twist animate the pattern; beat fires hue jump and brightness flash; saturation scales with loudness (near-monochrome at silence → vivid colour at full volume); offscreen pixel buffer at ¼ res (⅙ mobile) with imageSmoothingEnabled; sliders: Frequency (spatial density), Twist (audio angular deviation), Drift (animation speed)
│   ├── voronoi.ts             # Voronoi Stained Glass: N Voronoi cells mapped to 7 freq bands; each cell glows with jewel-tone hue (amethyst→sapphire→aquamarine→emerald→topaz→amber→ruby) at band amplitude; dark lead lines between cells; beat fires warm flash + Shatter scatters seeds then eases them home for kaleidoscope-click reorganisation; offscreen pixel buffer at ¼ res (⅛ mobile); sliders: Cells (10–80), Glow (light intensity), Shatter (beat scatter)
│   ├── grayscott.ts           # Gray-Scott: continuous A/B reaction-diffusion chemical simulation; Du=1.0/Dv=0.5 diffusion with per-cell feed/kill kinetics; ping-pong Float32Array buffers; sub-bass nudges feed rate, brilliance nudges kill rate, transients inject random activator seeds, beats inject central blob + hue jump; B concentration → HSB color; offscreen pixel buffer at ¼ res (⅙ mobile); sliders: Feed (F ∈ 0.010–0.095), Kill (K ∈ 0.040–0.075), Speed (steps/frame 1–6)
│   ├── blobs.ts               # Blobs: audio-reactive metaball field inspired by Zachary Lieberman's "Circles, Blobs, Ripples" (Unit London, 2024); 7 metaballs (one per freq band) drift via Perlin noise, each ball's charge driven by its band's amplitude; classic 1/r² scalar field thresholded to produce organic fusion; blobs merge into one heaving mass at high amplitude and scatter as separate orbs when quiet; beat bursts all blobs radially outward; offscreen pixel buffer at ¼ res (⅙ mobile); sliders: Viscosity (fusion threshold), Drift (animation speed), Glow (edge halo intensity)
│   ├── growth.ts              # Growth: differential growth simulation inspired by Nervous System's Floraform (2014); 7 closed curves (one per freq band) start as small circles and grow by inserting midpoint nodes on long edges; spatial-hash repulsion between all nodes causes curves to fold like coral, brain sulci, and leaf margins; amplitude drives growth speed and neon glow; beats sprout new seed organisms; 3-pass glow rendering; sliders: Growth (edge-split rate), Tension (spring stiffness), Repulsion (repulsion radius)
│   ├── pixelsort.ts           # Pixel Sort: glitch-art column pixel sorting inspired by Kim Asendorf's "Mountain Tour" (2010); synthetic plasma field (7 freq-band hue zones) sorted column-by-column by luminance; bass lowers sort threshold for more chaos; beat transients inject a sort surge; offscreen pixel buffer at ⅓ res (⅙ mobile); sliders: Threshold (sort luminance cutoff), Span (max sorted run length), Hue (palette rotation)
│   ├── echoes.ts              # Echoes: polar slit-scan temporal mandala inspired by Golan Levin's slit-scan video-art catalog (flong.com); each audio frame captured as an annular ring in a rolling buffer; rings radiate outward (newest at centre, oldest at edge); Twist morphs from concentric mandala to tight spiral; beat shifts hue palette + flash; 7 band hues cycle violet→red; sliders: Depth (ring history 20–150), Twist (rotation/ring, 0=mandala→1=spiral), Scale (amplitude-to-radius)
│   ├── physarum.ts            # Physarum: slime-mold agent-network simulation inspired by Zach Lieberman's 2025 daily sketches (Jeff Jones algorithm); N agents sense pheromone trails at 3 probes, steer toward highest concentration, deposit trail; 3×3 diffuse + evaporate each frame; emergent self-organising web of mycelial highways; beat scatters 20 % of agents + hue jump; bass drives deposit rate; dominant band colours hue; auto-normalised brightness; ⅓ res pixel buffer (⅕ mobile); sliders: Agents (100–3000), Evaporation (trail persistence), Sensor (angle 5°–75°)
│   └── optical.ts             # Op-Art: audio-reactive Op-Art grid inspired by Victor Vasarely's "Vega-Nor" series (1969); NxN grid of circles whose radii are modulated by 7 per-band sinusoidal standing waves (one per frequency, each at an increasing spatial frequency and a differently rotated axis) plus a central Vasarely dome illusion; beat fires a radial shockwave that briefly swells circles as it expands; Color slider morphs from pure black/white monochrome to full chromatic HSB palette; sliders: Grid (density 6–32), Bulge (distortion amplitude), Color (monochrome → chromatic)
│   ├── geodesic.ts            # Geodesic: up to 5 nested icosahedral wireframe shells rotate independently (each driven by a different audio frequency band); neon 3-pass glow per edge; vertex node dots; beat fires angular impulses; desktop uses once-subdivided icosphere (120 edges/shell), mobile uses base icosahedron (30 edges/shell); sliders: Shells (1–5 nested spheres), Spin (rotation speed), Glow (neon intensity)
│   ├── ribbons.ts             # Ribbons: N sinuous silk-ribbon strands spanning the canvas; each strand driven by a freq band; amplitude scales oscillation height; 3-pass iridescent gradient glow via canvas LinearGradient; beat fires a traveling snap wave; mobile guard (≤8 strands, 40 segs); sliders: Ribbons (strand count 2–12), Wave (oscillation density 0.2–4), Shimmer (iridescent hue-shift 0–1)
│   ├── infinitynet.ts         # Infinity Net: dense hex-offset grid of semicircular arcs inspired by Yayoi Kusama's "Infinity Net" paintings; 7 radial zones map to freq bands (sub-bass at centre → brilliance at edge); amplitude pulses arc size; beat fires expanding ripple ring; Palette morphs from Kusama white-on-black monochrome to full chromatic; sliders: Scale (grid density), Breathe (amplitude swell 0–1), Palette (monochrome→chromatic)
│   ├── zengarden.ts           # Zen Garden: karesansui (枯山水) dry-rock-garden visualization; rake lines flow across a warm sandy canvas curving around stone formations; 7 frequency bands each govern a horizontal zone (sub-bass=deep rolling waves, brilliance=fine ripples); beat triggers a phase-jump re-raking event; inspired by Ryōan-ji temple garden and Tokujin Yoshioka's sand installations; sliders: Lines (rake density 10–60), Depth (wave amplitude), Rocks (stone count 0–6)
│   ├── arabesque.ts           # Arabesque: 7 arabesque petals traced via John Whitney Sr.'s differential motion formula (r[k] = R·|sin(kπ/N)| at angle k·φ); petals phase-offset by 2π/7 for 7-fold symmetry; trail buffer accumulates layered Islamic-geometric forms; beat snaps phase and shifts hue palette; inspired by John Whitney Sr., "Arabesque" (1975); sliders: Steps (harmonic density 20–200), Speed (φ rate), Trail (persistence)
│   ├── murmuration.ts         # Murmuration: boid flocking visualization (key: |, Shift+\); N birds (100–3000) with separation/alignment/cohesion; each bird colored by velocity direction (hue = heading angle) producing rainbow swirls; persistent trail buffer; beat triggers "predator" edge-event + hue palette jump; sub-bass → separation, bass → cohesion, mid → alignment, presence → speed; inspired by Universal Everything "Future Self" (2012); sliders: Birds (population), Cohesion (cluster tightness), Trail (persistence)
│   ├── facets.ts              # Facets: jittered triangular mesh spanning the canvas; 7 freq bands drive face brightness and gem-hue (amethyst→sapphire→aquamarine→emerald→topaz→amber→ruby, left→right); two orbiting virtual light sources sweep caustic highlights; Perlin noise undulates vertices; beats burst vertices radially outward + flash; inspired by Quayola "Strata" (2010); sliders: Density (grid resolution), Shimmer (vertex displacement), Glow (edge brightness)
│   ├── epicycles.ts           # Epicycles: Fourier series harmonic visualizer inspired by 19th-century harmonograph machines; 7 nested spinning circles (one per freq band) rotate at integer multiples of a base speed; the tip traces a Lissajous-like curve in a ring-buffer trail rendered with 3-pass phosphor glow; bass drives the outermost arm, brilliance makes fine interior spirals; beat fires hue palette jump + brightness flash; mobile trail cap 400; sliders: Cycles (2–7 circles), Speed (rotation rate), Trail (history length)
│   ├── halftone.ts            # Halftone: three CMY dot-screens at 15°/45°/75° composited via MULTIPLY blend on white; overlapping inks combine (C+M→indigo, M+Y→scarlet, C+Y→olive) replicating offset-press colour mixing; dot radius driven by 7 freq bands mapped left→right; beat fires expanding ring-pulse of swollen dots; inspired by Roy Lichtenstein "Drowning Girl" (MoMA, 1963) / Ben-Day printing; sliders: Grid (dot density), Spread (amplitude sensitivity), Palette (CMYK→sunset→day-glo)
│   ├── circuit.ts             # Circuit: N nodes scatter across the canvas connected by PCB-style L-shaped bezier traces; each trace colored by its freq band (violet→magenta); signal pulses animate along traces; beat fires burst of pulses from random nodes + hue shift; 2-pass neon glow (halo + core); mobile guard (fewer nodes/pulses); inspired by Joshua Davis "Praystation" circuit-board generative patterns (2001–2003); sliders: Nodes (10–60), Speed (pulse velocity), Glow (neon intensity)
│   ├── caustics.ts            # Caustics: audio-reactive water light caustic patterns; 7 sinusoidal wave layers (one per freq band) at evenly distributed angles; constructive interference concentrates light into bright focal clusters on a deep-navy background; power-law sharpening creates razor caustic lines; radial vignette adds pool-depth feel; beat fires brightness surge + hue shift; ¼-res pixel buffer (⅛ mobile); inspired by Jason Bruges Studio light-caustic installations and Izabela Pluta "Like folds in water" (2024); sliders: Wave Scale (pattern density), Speed (shimmer rate), Brightness (focal sharpness)
│   ├── knots.ts               # Knot: torus knot T(2, q) rendered as 3 interleaved neon strands in pure p5.js 2D perspective (no Three.js); q oscillates between trefoil (3) and septafoil (7) as a beat-locked ease-out animation — every Nth beat (Beat Frequency slider) flips q's direction of travel so the knot snap-then-settles into a new shape on each beat; each strand colored by a freq register (sub+bass→violet, mid→teal, high→amber) with 60° hue gradient; 3-pass phosphor glow; beat fires angular impulse + hue shift; amplitude inflates torus radii; inspired by George W. Hart's mathematical sculpture at the Bridges Conference; sliders: Beat Frequency (direction-flip subdivision 1–8), Glow (phosphor intensity), Speed (orbit rate)
│   ├── penrose.ts             # Penrose: aperiodic rhombus tiling generated by recursively deflating a "sun" of 10 golden triangles via the Robinson-triangle substitution behind Penrose's P3 tiling; whole lattice slowly rotates; 7 concentric radial zones map distance-from-centre to freq bands (sub-bass=core → brilliance=rim) driving each tile's hue/brightness; beat fires an outward "deflation wave" flash + hue shift; inspired by the 2023 discovery of "the hat"/"spectre" aperiodic monotiles (David Smith, Craig Kaplan, Joseph Myers, Chaim Goodman-Strauss) and the renewed fascination with Penrose's 1974 never-repeating tilings it sparked; sliders: Density (recursion depth 3–7), Spin (rotation speed), Glow (brightness/bloom intensity)
│   ├── flame.ts               # Fractal Flame: audio-reactive iterated-function-system "chaos game"; a point jumps between 7 affine transforms (one per freq band) blended with nonlinear variations (sinusoidal, swirl, spherical, horseshoe, polar); millions of jumps accumulate into a luminous density+hue buffer rendered with exponential log-density tone mapping; loud bands dominate transform-selection weight and inflate their lobe, transient punches flare it brighter; beats mutate the transform "genes" — an homage to Scott Draves' Electric Sheep genetic-algorithm flame evolution; offscreen pixel buffer at ¼ res (⅛ mobile) with persistent decay; sliders: Density (chaos-game iterations), Glow (tone-mapping brightness), Mutation (beat-triggered gene-mutation strength)
│   ├── disorders.ts           # Disorders: minimalist ink-on-dark lattice of straight strokes inspired by Vera Molnár's "(Des)Ordres" (1974) and "Interruptions" (1968–69); grid starts perfectly aligned along one diagonal; 7 vertical zones map to freq bands (sub-bass=left → brilliance=right) and each band's amplitude rotates its zone's strokes toward their own fixed random angle; beats fire expanding circular "interruption" waves that erase and reseed a ring of strokes as they sweep past; sliders: Grid (lattice density), Disorder (baseline rotational chaos 0=ordered hatch→1=full chaos), Interrupt (strength/spread of beat-triggered erasure waves)
│   ├── blackwave.ts           # Black Wave: layered translucent wave silhouettes with parallax depth, inspired by teamLab's "Black Waves" (https://www.teamlab.art/w/black_waves/) and Hokusai's "The Great Wave off Kanagawa" (c. 1831); 7 freq bands drive nested wave layers from distant brilliance swells to a crashing sub-bass front wave; ukiyo-e "bokashi" gradient sky, dry-brush ink strokes along each crest, and foam particles spray on beats; sliders: Density (ink-stroke/foam richness), Swell (wave amplitude), Hue (sumi-e monochrome → indigo ai-zuri → vermillion Red Fuji)
│   ├── origami.ts             # Origami: audio-reactive Miura-ori herringbone pleat tessellation inspired by Issey Miyake's "132 5." origami-based clothing line (Reality Lab + Jun Mitani, 2010, https://www.dezeen.com/2010/10/05/132-5-by-issey-miyake/); a grid of parallelogram facets is displaced into alternating peak/valley folds, 7 columnar zones map sub-bass (left) through brilliance (right) to fold depth, per-facet normal/light shading catches highlights and shadows on each pleat; beat fires a diagonal fold wave that ripples once across the sheet; sliders: Fold (base pleat depth), Grid (tessellation density 6–24), Wave (beat fold-wave speed/reach)
│   ├── lightfield.ts          # Light Field: drifting 3D lattice of luminous orbs inspired by Squidsoup's "Submergence" / "Ocean of Light" suspended-LED installations (https://www.squidsoup.org/portfolio/submergence-2/, https://www.thisiscolossal.com/2013/01/submergence-an-immersive-field-of-8064-suspended-lights-by-squidsoup/); each of 7 freq bands drives a plane wave of brightness through the lattice along its own 3D direction/frequency, with Perlin-noise jitter giving each point a gentle independent drift; an orbiting tilted camera projects the lattice to 2D with perspective scaling; beats fire an expanding spherical pulse shell that sweeps brightness outward through the points; 3-pass additive glow per point, deep-ocean blue→cyan→violet/magenta palette by dominant band; sliders: Grid (lattice density), Flow (wave speed/drift turbulence/camera orbit speed), Glow (halo brightness)
│   ├── brush.ts               # Brush: generative action painting inspired by Joan Mitchell's "La Grande Vallée" series (1983-84); autonomous bezier-curve brush strokes spawned by beats and audio energy; each of 7 freq bands maps to a hue (warm reds/oranges for bass → cool blues/violets for highs); Perlin-noise-curved strokes animate tip-to-tail with 3-pass neon glow (outer halo / mid / core); beat shifts hue palette and spawns bold gestural strokes; transients trigger spontaneous marks; amplitude-driven paint splatter particles; offscreen trail buffer with configurable fade; mobile guard (120 strokes / 24 pts); sliders: Strokes (spawning rate), Weight (brush thickness), Trail (paint persistence)
│   ├── aurora.ts              # Aurora: flowing luminous curtains of light inspired by Refik Anadol's "Infinity Room" data sculpture at Dataland (2026); layered ribbons of light sweep across canvas like aurora borealis; each of 7 layers driven by a freq band controlling brightness, oscillation amplitude, and color; Perlin noise organic undulation; beat surge + hue shift; additive-blend trail buffer; mobile guard (½ res, wider step); sliders: Flow (speed/turbulence), Layers (curtain count 3–12), Glow (halo intensity)
│   ├── glitch.ts              # Glitch: analog/VHS glitch art inspired by Nam June Paik's "Global Groove" (1973) and "Magnet TV" (1965); 7 vertical color bars (one per freq band) form a base signal; bass bends scanlines horizontally, mid-range splits RGB channels, treble injects static grain; beats trigger tracking-loss bars and displaced block artifacts; CRT scanline overlay; offscreen pixel buffer at ⅓ res (⅕ mobile); sliders: Distort (horizontal displacement), Split (RGB channel separation), Noise (static grain density)
│   ├── warp.ts                # Warp: cosmic hyperspace starfield inspired by Ryoji Ikeda's "data.tron" (2007); stars zoom outward from center with 3D→2D perspective projection; each star colored by its freq band (violet→blue→teal→green→yellow→orange→magenta); amplitude drives warp speed; beats trigger burst acceleration + hue palette shift; trail buffer with configurable fade for persistent streaks; close stars get glow halo; mobile guard (600 vs 1200 stars); sliders: Speed (base warp velocity), Density (star count), Trail (streak persistence)
│   ├── vortex.ts              # Vortex: audio-reactive spiral galaxy inspired by Olafur Eliasson's "Your Rainbow Panorama" (2011, ARoS Aarhus Kunstmuseum, https://olafureliasson.net/artwork/your-rainbow-panorama-2011/); N logarithmic spiral arms emanate from canvas centre, each mapped to a freq band (cycling if arms > 7); amplitude drives arm brightness and width; 3-pass additive glow rendering (outer halo + mid + core); beat fires rotation impulse + hue palette shift; offscreen trail buffer accumulates layered mandala-like patterns; centre glow pulses with overall amplitude; mobile guard (fewer points per arm); sliders: Arms (spiral arm count 2–12), Twist (spiral tightness), Speed (rotation rate)
│   ├── lumia.ts               # Lumia: flowing translucent color forms with additive blending inspired by Thomas Wilfred's "Lumia" light art and his Clavilux color organ (1920s-30s, https://americanart.si.edu/exhibitions/lumia); multiple large soft luminous shapes drift across canvas via Perlin noise; each of 7 freq bands drives one or more forms; forms are translucent and blend additively where they overlap creating rich secondary colours; amplitude scales form size and brightness; beats trigger expansion pulse + hue palette shift; multi-stop radial gradients with inner iridescent glow layer; offscreen trail buffer at ½ res (mobile); sliders: Forms (luminous shape count 4–14), Drift (movement speed/turbulence), Glow (brightness/trail persistence)
│   ├── woodmirror.ts          # Wood Mirror: audio-reactive mechanical-pixel relief inspired by Daniel Rozin's "Wooden Mirror" (1999) kinetic mirror sculptures (bitforms gallery, https://www.bitforms.art/artist/daniel-rozin); grid of wooden tiles holds a relief height per cell, 7 freq bands drive column-zone relief via Gaussian weighting (sub-bass=left → brilliance=right), Perlin noise adds organic per-tile drift; surface normal derived from height gradient and lit by one fixed light source (no color audio mapping — only tilt/shading, true to Rozin's mechanical pixels); beats send a circular wave of motion across the grid; sliders: Density (grid resolution), Depth (relief amplitude), Speed (motor response speed)
│   ├── moire.ts               # Moiré: Bridget Riley op-art inspired; two layered fields of concentric rings drift apart from each other (centres pushed by sub/bass/low-mid/mid amps + slow Lissajous sway) generating moving moiré interference bands; a third layer of radial spokes rotates at upper-mid-driven speed; palette flips through Riley-style dual-hue combos on every beat with a hue-tinted flash; ADD blend for op-art glow; distinct from `interference.ts` (Ryoji Ikeda barcode-stripes) — this is concentric-ring op-art; inspired by Bridget Riley "Blaze" (1964) and "Current" (1964); sliders: Rings (8–80), Interference (drift aggression), Contrast (line thickness); keyboard: f
│   ├── noctiluca.ts           # Noctiluca: bioluminescent sea sparkle cascade; N particles (1800 desktop/700 mobile) drift through Perlin noise currents representing Noctiluca scintillans dinoflagellates; beat-triggered circular shockwaves propagate outward from canvas edges, illuminating each particle as the wavefront passes — recreating the "blue fire" ocean phenomenon; 7 freq bands map to distinct hues (indigo→blue→cyan→teal→seafoam→chartreuse); additive blending with per-frame dark overlay creates organic afterglow trails; inspired by Studio DRIFT "Franchise Freedom" (2017) drone light swarms; sliders: Drift (current speed), Bloom (glow halo radius/trail persistence), Wake (wave brightness/cascade intensity); keyboard: :
│   └── tesseract.ts           # Tesseract: rotating 4D hypercube (tesseract) wireframe; 16 vertices and 32 edges projected from 4D to 2D via double perspective division; 6 rotation planes (XY, XZ, XW, YZ, YW, ZW) each driven by a different frequency band, creating impossible-looking inner-through-outer "phase-passing" projections unique to 4D geometry; up to 5 nested cubes with rainbow-hue palette (violet→blue→teal���lime→magenta); 3-pass neon glow per edge; vertex nodes at each projected corner; beats fire angular impulses; inspired by Tony Robbin's "Fourfield: Computers, Art and the 4th Dimension" (1992, https://www.tonyrobbin.net/fourfield.htm) and Henry Segerman's mathematical 4D visualization art (https://henryseg.github.io/); sliders: Layers (1–5 nested tesseracts), Spin (rotation speed), Glow (neon intensity); keyboard: T (Shift+t)
│   └── supershapes.ts         # Super Forms: 7 concentric Gielis superformula curves (one per freq band) nest at canvas centre; sub-bass anchors the outermost ring, brilliance the innermost; n₁ parameter (roundness vs spikiness) driven by amplitude — quiet = smooth polygon, loud = spiky starburst; shapes rotate alternating CW/CCW at audio-modulated speed; Perlin noise drifts n₂/n₃ for organic asymmetry; beat fires simultaneous topology jump (new integer m = lobe count for all shapes) + hue palette shift; 3-pass neon glow + trail buffer; inspired by Johan Gielis "A Generic Geometric Transformation…" (American Journal of Botany, 2003, https://doi.org/10.2307/4117014) and Michael Hansmeyer's "Digital Grotesque" (2013, Centre Pompidou); sliders: Symmetry (m lobe-count range 2–18), Morph (amplitude sensitivity), Glow (bloom + trail persistence); keyboard: V (Shift+v)
│   └── corridor.ts            # Corridor: infinite data-mirror tunnel; N receding rectangular rings form a first-person corridor perspective; genomic/binary characters (ATCG + 01) scroll along ceiling and floor strips of each ring; 7 horizontal column-zones map to freq bands (sub-bass=violet left → brilliance=magenta right); amplitude drives character brightness and scroll speed; near rings scroll faster for parallax effect; neon pillar lines connect adjacent rings implying mirror walls; radial vanishing-point glow intensifies with amplitude; beat fires "step-forward" zoom burst + white flash + hue shift; inspired by Ryoji Ikeda "the critical paths" (2024, Estonian National Museum, Tartu, https://vimeo.com/1035453913); sliders: Speed (scroll rate), Depth (ring count 4–16), Palette (monochrome → full band colours); keyboard: Q (Shift+q)
│   └── riemann.ts             # Riemann Sphere: stereographic projection of a rotating sphere; a unit sphere carries 7 glowing latitude circles (one per freq band) and meridian grid lines that project onto the canvas from the north pole — upright sphere yields concentric rings, tilted sphere yields eccentric orbiting circles; sub-bass drives the outermost ring, brilliance the innermost; beat fires rotation impulse and 47° hue palette shift; additive blending for luminous glow; inspired by Henry Segerman's stereographic projection lamp sculptures (Oklahoma State University, https://math.okstate.edu/people/segerman/); sliders: Spin (rotation speed), Tilt (0=concentric rings → 1=maximum eccentric orbit), Glow (stroke weight/bloom)
│   └── glyphs.ts              # Glyphs: scattered field of ~100–500 typographic glyphs (Greek letters, math symbols, numerals, Latin capitals) drifting via Perlin noise; each glyph's horizontal canvas position maps it to one of 7 freq bands; band amplitude drives glyph size, brightness, and angular spin; beats scatter glyphs outward and shuffle ~40 % of characters; canvas shadowBlur glow; inspired by Ksawery Kirklewski's "Symphony in Acid" (2022, https://maxcooper.net/symphony-in-acid); sliders: Density (glyph count), Scale (base size/amplitude sensitivity), Drift (Perlin-noise speed/chaos); keyboard: g
│   └── prism.ts               # Prism: thin-film iridescent light field; a drifting Perlin-noise field of "soap-film thickness" values drives optical thin-film interference coloring via a 512-entry spectral LUT (path difference 2·n·t selects which wavelengths constructively interfere, producing the characteristic Newton's-rings soap-bubble colour sequence); 7 freq bands impose standing thickness waves at increasing spatial frequencies (sub-bass = large blobs, brilliance = fine shimmer); beats fire a radial thickness shockwave and shift the palette phase; ¼-res pixel buffer (⅛ mobile); inspired by Tokujin Yoshioka "Rainbow Church" (500 000 Swarovski crystal prisms, Design Miami 2010, https://www.tokujin.com/work/rainbow) and Fabian Oefner's "Iridient" soap-bubble photography series (2012); sliders: Film (base thickness 0=thin/monochrome → 1=full spectral cycling), Flow (drift speed/grain density), Shimmer (iridescence brightness); keyboard: P (Shift+p)
│   └── cloth.ts               # Tapestry: Verlet-integration cloth simulation; a grid of spring-connected particles hangs from the top edge; 7 column-zone freq bands drive audio forces in the depth (z) axis making the surface ripple toward/away from the viewer; per-triangle Phong shading with two virtual lights gives each tile metallic glint; iridescent hue shifts with surface normal angle; beat fires a wide z-impulse wave + hue palette shift; 30×26 desktop / 18×16 mobile; inspired by El Anatsui's "Bleeding Takari II" (2007, MoMA, https://www.moma.org/collection/works/116286) — thousands of aluminium bottle-caps and copper wire draped like shimmering cloth; sliders: Drape (gravity 0=levitating→1=heavy canvas), Ripple (audio-force amplitude), Shimmer (metallic iridescence + specular brightness); keyboard: W (Shift+w)
│   └── veil.ts                # Veil: N luminous filament threads drift through the canvas collectively forming emergent sculptural shapes — the sculpture exists only through their combined behaviour; 7 frequency-band vortex attractors orbit slowly on a heptagon, each spinning in alternating CW/CCW direction; at high amplitude threads crystallise into braided vortex formations, at silence they diffuse into free Perlin smoke; additive-blend segments to a ½-res trail canvas with configurable fade; beat fires hue shift + crystallisation surge; violet→blue→teal→green→lime→orange→magenta palette; inspired by teamLab "Light Sculpture – Flow" (teamLab Borderless, Azabudai Hills Tokyo, 2024, https://www.teamlab.art/w/lightsculpture-flow/); sliders: Threads (filament count), Order (crystallisation vs. smoke drift), Trail (persistence); keyboard: D (Shift+d)
│   └── suminagashi.ts         # Suminagashi: 7 luminous ink sources (one per freq band) drift via Perlin noise across a dark canvas; each source drops expanding concentric rings whose rate is driven by band amplitude; rings blend additively where they cross — iridescent hue mixing mirroring real ink-on-water; beat fires a radial breath from canvas centre pushing sources outward + hue palette jump; 3-pass glow per ring (outer halo, mid, bright core); offscreen trail buffer with bloom-controlled fade; inspired by the ancient Japanese art of floating ink marbling practiced since the Heian period (794–1185 CE) and contemporary artisans at Suminagashi Studio (https://suminagashi.com/); sliders: Rings (spawn rate), Drift (source navigation speed), Bloom (glow halo size + trail persistence); keyboard: U (Shift+u)
│   └── isometric.ts           # Isometric City: audio-reactive axonometric cityscape; 14×14 grid (8×8 mobile) of buildings rendered with classic 2:1 isometric projection; 7 radial zones map frequency bands (sub-bass at centre → brilliance at outer ring) to building heights; beat fires a height-wave ripple outward from the grid centre + hue palette shift; three-face shading (roof, left wall, right wall) with painter's algorithm depth ordering; neon glow on rooftops in night mode; Palette slider morphs from neon cyberpunk night to warm daylight pastels; inspired by teamLab "Living Digital Space and Future Parks" (https://www.teamlab.art/e/planet/) and Santiago Torres isometric illustration style; sliders: Density (city zoom — 0=close-up towers, 1=wide skyline), Height (building amplitude sensitivity), Palette (neon night→daylight); keyboard: 8
│   └── pursuit.ts             # Pursuit: 7 packs of N particles arranged on a regular N-gon each chase the next in the cycle, tracing logarithmic spirals (the classical "mice problem"); one pack per freq band — band amplitude drives pursuit speed; packs converge and re-seed at Perlin-noise positions; beat fires all packs radially outward; additive-blend trail buffer builds mandala-like whorls at high persistence; inspired by Pierre Bouguer's pursuit curves (1732) and the Genuary 2024 creative-coding community (https://genuary.art/); sliders: Symmetry (polygon order N 3–12), Speed (chase velocity), Trail (persistence); keyboard: H (Shift+h)
│   └── arboreal.ts            # Arboreal: L-system botanical branching visualization; recursive string-rewriting (Axiom F, Rule F→F[+F][-F]) generates self-similar coral/tree geometry; each recursion depth level maps to a freq band — sub-bass drives the thick trunk, brilliance animates finest tips; bass-heavy music builds towering trunks, treble-rich passages proliferate delicate canopy; 3-pass neon glow with ADD blending; trail buffer persistence; beat fires hue palette shift + brightness flash; inspired by Prusinkiewicz & Lindenmayer "The Algorithmic Beauty of Plants" (1990, http://algorithmicbotany.org/) and Robert Hodgin's "Growth v02" (fxhash, https://www.fxhash.xyz/generative/27022); sliders: Depth (recursion depth 2–6), Spread (branching angle 12°–55°), Glow (bloom + trail persistence); keyboard: A (Shift+a)
│   └── etching.ts             # Etching: audio-reactive engraving visualization; 7 vertical zones (sub-bass left → brilliance right) each filled with sinusoidally oscillating horizontal strokes; denser, more agitated lines indicate higher amplitude; colors follow violet→blue→teal→green→yellow→orange→magenta per band; beat fires synchronised phase-jump snapping all zones to new oscillation angles; inspired by Albrecht Dürer's copper-plate engravings (c. 1507–1528) and Tyler Hobbs' generative pen-plotter hatching work in "Fidenza" (2021); sliders: Lines (stroke density 3–20), Wave (spatial oscillation 0=gentle→1=scribble), Glow (halo brightness); keyboard: i
│   └── kintsugi.ts            # Kintsugi: audio-reactive golden repair visualization; beats shatter the dark ceramic canvas with branching fracture lines that glow from within with molten gold light; sub-bass produces wide deep fissures while brilliance creates fine hairline tracery; 3-pass ADD-blend neon glow per segment; spontaneous hairline cracks from sustained high amplitude; mic-mode transient detection; inspired by the traditional Japanese art of kintsugi (金継ぎ) and teamLab's "Transcending Boundaries" (Mori Art Museum, Tokyo 2020); sliders: Fragility (crack density on beats), Gold (color temperature amber→gold→silver), Trace (crack persistence); keyboard: K
│   └── klimt.ts               # Klimt: audio-reactive golden mosaic; dense grid of ornamental tiles fills the canvas with five Art Nouveau motifs (nested squares, circles with radiating spokes, almond eyes/mandorla, equilateral triangles, Archimedean spirals) distributed deterministically; 7 column zones map frequency bands left-to-right (sub-bass → brilliance) driving each tile's brightness and scale; beat fires an expanding golden shimmer ring from canvas centre; warm gold palette (HSB 25–65) inspired by Gustav Klimt's "The Tree of Life" (1905-09, Stoclet Frieze) and the Byzantine mosaic tesserae of Ravenna's San Vitale (c. 547 CE) that Klimt visited in 1903; sliders: Tile Size (grid density), Gold (palette warmth/saturation), Complexity (ornamental intricacy); keyboard: B
│   └── rosette.ts             # Rosette: 7 hypotrochoid spirograph curves (one per freq band) drawn as complete closed loops; band amplitude drives pen-extension d from collapsed circle to full petal bloom; curves rotate at slightly different speeds creating mandala-like interference patterns; beat fires hue jump + petal-count snap; 3-pass phosphor glow on offscreen trail buffer; inspired by guilloche engine-turning tradition (Fabergé enamelwork, 1885–1917) and Reza Ali's parametric curve generative art on fxhash; sliders: Petals (base loop count 3–8), Bloom (pen-extension scale), Glow (trail persistence); keyboard: J
│   └── feedbackloop.ts        # Feedback: video feedback loop simulation; each frame the previous frame is scaled and rotated slightly before 7 frequency-band concentric rings are drawn on top; recursive accumulation creates converging spirals whose hue history is visible as concentric colour layers; ping-pong OffscreenCanvas buffers; additive ("lighter") blend for ring glow; beat fires hue-palette jump + central brightness flash; inspired by Steina & Woody Vasulka "Violin Power" (1970–78), one of the earliest audiovisual works using video feedback driven by sound; sliders: Zoom (feedback scale — tight vortex vs slow fade), Spiral (rotation per frame — concentric rings vs tight spiral), Glow (ring stroke weight and bloom); keyboard: <
│   └── kandinsky.ts           # Kandinsky: audio-reactive synesthetic color-form composition; 7 freq bands each map to a Kandinsky color-tone affinity (sub=violet/spirit, bass=yellow/trumpet, lowMid=orange, mid=red/drum, upperMid=green/violin, presence=blue/cello, brilliance=white) and one geometric form type (triangle, circle, square, arc, cross, chevron, star); forms drift via Perlin noise, scale with band amplitude, and accumulate on an offscreen trail buffer building layered painterly compositions; 3-pass additive neon glow; beat fires hue-palette shift and scatters forms radially; inspired by Wassily Kandinsky "Composition VIII" (1923, Solomon R. Guggenheim Museum, https://www.guggenheim.org/artwork/1924) and "Concerning the Spiritual in Art" (1911); sliders: Forms (forms per band 3–20), Chaos (Perlin turbulence — calm drift → kinetic scatter), Glow (bloom intensity and trail persistence); keyboard: C
│   └── cobweb.ts              # Cobweb: audio-reactive spider-web visualization; N radial spokes from a central anchor connected by M concentric rings; 7 freq bands drive ring brightness and strand vibration (sub-bass at centre → brilliance at rim); dew-drop nodes glow at spoke–ring intersections proportional to band amplitude; beat fires an expanding radial pulse wave + hue-palette shift; 3-pass neon glow per strand; inspired by Tomas Saraceno's "In Orbit" (2013, Kunstsammlung Nordrhein-Westfalen, Düsseldorf, https://tomassaraceno.com/projects/in-orbit/); sliders: Density (spoke/ring count), Dew (intersection glow), Tension (vibration amplitude); keyboard: E
│   └── frost.ts               # Frost: audio-reactive ice-crystal dendritic growth; N-fold crystallographic symmetry (3–12 configurable folds) grows fractal branch tips from one or more nucleation centres; 7 freq bands steer tip extension speed in their angular sectors; tips fork at 60° (or fold-appropriate) angles when audio amplitude spikes; segments accumulate on a persistent trail buffer (slow-fading frost on cold glass); 3-pass neon glow per segment (outer halo, mid bloom, crisp ice-white core); beat triggers satellite crystal nucleation or full thaw-and-refreeze; inspired by Alexey Kljatov's macro snowflake photography (https://chaoticmind75.blogspot.com/) and Tokujin Yoshioka "VENUS – Crystal Nature" (2022, Kyushu National Museum); sliders: Symmetry (fold count 3–12), Growth (branch density/speed), Glow (bloom + trail persistence); keyboard: L
│   ├── boogie.ts              # Boogie Woogie: audio-reactive Mondrian grid; canvas divided into a frequency-band-mapped city-block grid (coarse Mondrian squares → fine Manhattan street grid); 7 freq bands (sub-bass=left → brilliance=right) drive the saturation/brightness of scattered coloured city blocks (classic red/yellow/blue primary palette → dark neon); golden streets (thick grid lines) carry small coloured "boogies" — squares that race outward from the canvas centre on every beat like taxis flying away from Times Square; Palette slider morphs from warm Mondrian oil-on-canvas to dark neon; inspired by Piet Mondrian "Broadway Boogie Woogie" (1942-43, MoMA, https://www.moma.org/collection/works/78682); sliders: Grid (density 6–20 divisions), Pulse (beat-burst strength), Palette (classic → dark neon); keyboard: M
│   └── patchwork.ts           # Patchwork: full-canvas grid of quilt blocks (Log Cabin, Half-Square Triangle, Pinwheel, Four-Patch, Rail Fence); 7 vertical column zones map sub-bass (left) → brilliance (right) to block brightness and hue; transients flash blocks white; beat fires an expanding ring that shuffles block patterns outward from canvas centre; warm folk quilt → cool graphic palette slider; optional pattern animation (breathing/rotation); inspired by Emily Xie "Interwoven" (2023, LACMA x Cactoid Labs, https://verse.works/series/interwoven-by-emily-xie); sliders: Grid (cell density), Palette (warm → cool), Motion (animation intensity); keyboard: N
│   └── risograph.ts           # Risograph: layered screen-print aesthetic; 7 ink layers (one per freq band) of bold flat-color geometric shapes (circles, ellipses, rounded rects, pills) rendered with additive blending — overlapping layers compound into vivid secondary hues and luminous whites; Perlin-noise field drifts shapes across the canvas; beat fires mis-registration offsets so layers briefly shift out of alignment, revealing the stacked color strata; inspired by Corita Kent "Handle with Care" (1967, https://coritakent.org/) serigraphy prints and the Risograph printing revival in independent art culture; sliders: Shapes (shapes per ink layer 1–4), Shift (mis-registration distance on beats), Bloom (additive glow halo intensity); keyboard: G
│   └── lensflare.ts           # Lens Flare: audio-reactive anamorphic lens flare field
│   └── radiolaria.ts          # Radiolaria: Ernst Haeckel "Kunstformen der Natur" (1904) inspired viz; N-fold radial symmetry with nested silica shells, two counter-rotating spoke sets forming a shifting diamond lattice, long bioluminescent spines; 6 bands drive shell radii (sub-bass=core → presence=rim); brilliance drives spine length; beat fires hue shift + expansion ring; sliders: Symmetry (6–24 folds), Shells (2–6 rings), Glow (bloom + trail persistence); 7 chromatic light sources (one per freq band) drift slowly in a ring; each emits a central bloom glow, four-point diffraction spikes, a horizontal anamorphic streak, and ghost bokeh circles along the flare axis through canvas centre; sources blend additively so simultaneous peaks create near-white convergence — the "chromatic sculpture" is colour in ordered space with no material boundary; beat fires hue shift + brightness flash; violet→blue→cyan→green→yellow→orange→red palette per band; inspired by teamLab "Chromatic Existence" (2026, Light Sculpture – Flow, teamLab Borderless Azabudai Hills Tokyo, https://www.businesswire.com/news/home/20260807256066/en/teamLab-Unveils-New-Series-from-Light-Sculpture---Flow-at-teamLab-Borderless); sliders: Streak (anamorphic streak length), Ghosts (ghost bokeh intensity), Bloom (central glow radius); keyboard: F
│   └── stitch.ts              # Stitch: audio-reactive cross-stitch embroidery grid; 7 freq-band column zones activate X-shaped thread marks on a dark Aida cloth canvas; stitches accumulate and decay (trail buffer with configurable persistence); beat fires an expanding radial stitch-wave from canvas centre like a needle jumping across fabric; per-cell brightness state array drives amplitude-proportional activation probability; neon phosphor glow per X mark; mobile guard (larger cells, reduced activation); inspired by Giorgia Lupi & Stefanie Posavec "Dear Data" (2016, MoMA collection, https://www.dear-data.com/); sliders: Grid (cell density 8–40 px), Trail (thread persistence), Glow (phosphor bloom); keyboard: O
│   └── magnetosphere.ts       # Magnetosphere: gravity-well particle orbit system; 7 frequency-band attractor nodes arrange on a slowly rotating heptagon ring, each pulling swarms of charged particles into fluid orbital compositions; attractor gravity scales with band amplitude — loud kick creates tight spiral orbits, silence releases particles into free drift; luminous comet trail buffer with configurable persistence; beats scatter all attractors radially outward then ease them back; 3-pass attractor glow; mobile guard (700 vs 2200 particles); inspired by Robert Hodgin "Magnetosphere" (2007, Apple iTunes 8 default visualizer, https://roberthodgin.com/project/magnetosphere); sliders: Particles (swarm density), Gravity (attractor pull), Trail (comet persistence); keyboard: X
│   └── substrate.ts           # Substrate: crystalline growth lines; cardinal-direction tips grow across a dark canvas and spawn perpendicular branches when they expire, creating emergent city-block / mineral-crystal formations; 7 freq bands drive growth activity in horizontal zones (sub-bass=left → brilliance=right); beat fires a radial burst of new crystal seeds from canvas centre + palette shift; gentle Perlin-noise jitter keeps lines organic; 3-pass neon glow on trail buffer; mobile guard (120 vs 350 max tips); inspired by Jared Tarbell "Substrate" (2003, http://www.complexification.net/gallery/machines/substrate/); sliders: Density (branching rate and tip population), Speed (growth velocity and line length), Glow (neon bloom and trail persistence); keyboard: S
│   └── ferrofluid.ts          # Ferrofluid: audio-reactive magnetic liquid spike field; hexagonally-packed grid of Rosensweig instability spikes rendered via stacked-ellipse depth illusion; 7 freq bands drive spike height across horizontal zones (sub-bass=left → brilliance=right); Perlin-noise drift gives each spike organic independence; iridescent thin-film sheen colours tips from dark monochrome to vivid rainbow; beat fires an expanding radial pressure wave that boosts all spikes it crosses + hue palette shift; 3-pass additive tip glow; inspired by Sachiko Kodama "Protrude, Flow" (2001, SIGGRAPH Art Gallery, https://www.sachikokodama.com/); sliders: Density (hex grid spacing — coarse few tall spikes vs fine many small), Height (amplitude sensitivity and max spike extension), Iridescence (monochrome ferrofluid → vivid rainbow shimmer); keyboard: Z
│   └── cataract.ts            # Cataract: Bridget Riley op-art wave bands; N horizontal sinusoidal boundary curves divide the canvas into alternating dark/light filled bands; a fixed per-curve phase offset creates the "cataract" waterfall optical-flow illusion; 7 frequency zones (left=bass → right=brilliance) drive local wave amplitude so the spectrum is spatially painted across the canvas; beat fires a half-cycle phase-flip that reverses apparent flow direction; Palette slider morphs from Riley's monochrome to full chromatic band-hue gradients; inspired by Bridget Riley "Cataract 3" (1967, British Council Collection, https://artuk.org/discover/artworks/cataract-3-219038); sliders: Lines (band count 5–40), Wave (amplitude sensitivity flat→deeply curved), Palette (monochrome→chromatic); keyboard: `
│   ├── tracery.ts             # Tracery: audio-reactive Gothic rose window; 7 concentric rings of stained-glass arc panels (one per freq band, inner sub-bass → outer brilliance) subdivided into N×(ring+1) panels that glow violet→amber with band amplitude; dark "lead-line" stone tracery dividers between panels; beat fires radial white flash + hue palette shift; slow rotation; inspired by Notre-Dame de Paris west rose window (c. 1220 CE) and Joan Vila-Grau's Sagrada Família stained glass (2010, https://www.sagradafamilia.org/en/decorative-arts); sliders: Petals (N-fold radial divisions 4–12), Glow (bloom intensity), Spin (rotation rate); keyboard: (
│   └── homage.ts              # Homage: N×N grid of Josef Albers-style nested-square colour studies; each composition = 3–9 flat concentric squares laid out in Albers' signature "bottom-heavy" offset (inner squares sit lower than centre); nesting levels map evenly to the 7 freq bands (outer = sub-bass, inner = brilliance) driving each layer's saturation and brightness; beats rotate the whole palette ladder outward one step so colours shift through the layered field like a slow chromatic pulse; per-tile phase offsets keep the mosaic from flashing in lock-step; Palette slider morphs between warm Albers "Solar" (deep brown → terracotta → ochre → ivory) and cool "Ascension" (midnight navy → teal → seafoam → lavender) 9-stop palettes; inspired by Josef Albers "Homage to the Square" series (1949–1976, Solomon R. Guggenheim Museum), notably "Homage to the Square: Apparition" (1959, https://www.guggenheim.org/artwork/106) and his teaching text "Interaction of Color" (1963, Yale University Press); sliders: Grid (1–4 tiles per axis; capped at 3 on mobile), Nesting (3–9 squares per composition), Palette (warm Solar → cool Ascension); keyboard: I (Shift+i)
│   └── protractor.ts          # Protractor: N fans of concentric arcs share the canvas centre, each sweeping a configurable angular sector; 7 spectral hues (one per freq band, violet→red) colour successive rings; band amplitude drives arc glow and stroke weight; the whole pattern slowly rotates with beat-driven impulses and hue palette shifts; 2-pass additive neon glow; trail buffer; mobile guard (capped fans and rings); inspired by Frank Stella "Protractor" series (1967–69, MoMA, https://www.moma.org/artists/5641); sliders: Fans (2–8 arc fan groups), Rings (4–24 concentric arcs per fan), Spread (angular sweep narrow wedge → nearly full circle); keyboard: _
│   └── phyllotaxis.ts         # Phyllotaxis: golden-angle seed packing lattice where N seeds are placed at successive multiples of the golden angle (≈137.508°), producing the canonical Fibonacci spiral arrangement found in sunflower heads, pinecones, and pineapple scales; 7 radial zones map frequency bands (sub-bass at core → brilliance at rim) to seed color and glow; band amplitude drives seed size; the whole lattice slowly rotates — at the right speed, Fibonacci spiral arms emerge and dissolve in the manner of Edmark's strobe sculptures; beat fires radial scatter burst then seeds ease home; 3-pass additive glow per seed; mobile guard (450 seeds max); inspired by John Edmark's kinetic "Blooms" sculptures (2014, https://www.instructables.com/Blooms-Fibonacci-Zoetrope-Sculptures/); sliders: Seeds (60–900), Spin (rotation rate), Bloom (amplitude sensitivity); keyboard: +
│   └── __tests__/             # Visualization tests (image drawing, userImage lifecycle)
├── midi/
│   ├── manager.ts             # Web MIDI API access, CC listener, mapping storage (localStorage), startMappingMode
│   └── ui.ts                  # MIDI overlay panel: Map/Clear buttons per slider, status badge, ESC close
├── ui/
│   ├── controller.ts          # Top-level UI orchestrator, sidebar toggle, viz selector, randomize, MIDI init
│   ├── splash.ts              # Splash screen: file upload, sample button, mic button, mode selector, image upload, play button
│   ├── playback.ts            # Pause/play, scrubber, time display, track switching, image controls, BPM trigger
│   ├── bpm.ts                 # BPM controls: number input (auto-populated), TAP tempo, BEAT phase sync
│   ├── sliders.ts             # Volume, 7-band frequency sensitivity, display sliders
│   └── keyboard.ts            # Keyboard shortcuts: arrows cycle vizzes (via viz-selector option order), space play/pause, ↑↓ volume, m/f/s/r/i/?/h/Esc utility. Viz hotkeys were removed — the `key` field on registry entries is currently unused, but kept for potential future reintroduction
├── types/
│   ├── index.ts               # Core interfaces: AppState, Config, VizMode, WormholeEvent, ActiveObject, AudioProcessingState, MidiMapping, etc.
│   └── globals.d.ts           # Global type stubs for p5.js and Tone.js (loaded from CDN)
└── utils/
    ├── constants.ts           # Frequency bands, octaves, FFT size, default config, mobile detection
    ├── errors.ts              # Error UI injection, processing overlay
    └── format.ts              # Time formatting
```

### Data flow

1. **Audio input** — User uploads a file, selects sample track, or uses microphone for live input (via `Tone.UserMedia`).
2. **BPM detection** — Client-side onset/autocorrelation from the decoded `AudioBuffer`.
3. **Viz reset** — On `audioReady`, `VIZ_REGISTRY.highway.reset?.()` clears highway state for the new track. All other vizzes are reset when the user switches to them via the registry's `reset` callback.
4. **Playback** — `audioEngine` creates a single Tone.js Player + FFT node for freq mode (or a Tone.UserMedia + FFT for mic mode).
5. **Render loop** (`main.ts` `p.draw`) runs at 60fps:
   - Get raw FFT → log-band amplitudes (7 bands)
   - Apply auto-gain normalization, transient detection, delta computation
   - Smooth with attack/release per band, frame-rate independent via `dt`
   - Store results in `store.audioState` (smoothedBands, transientValues, deltaValues)
   - Beat tracking: `floor(playbackPosition / beatInterval)` to detect beat changes
   - Dispatch to active visualization's draw function
5. **Visualization** — Each viz reads `store.audioState` and `store.config` directly. p5.js handles all 2D/3D rendering to a full-screen canvas.

### State management

`store` is a singleton `StateStore` with three state objects:
- **`state: AppState`** — mode (`freq`/`mic`/`interactive`), vizMode, isPlaying, BPM data
  - `interactive` mode: audio plays as background but FFT/BPM are skipped. Instead, `src/audio/interactiveSynth.ts` turns each pointer/key event into the same `audioState` (smoothedBands, transientValues, smoothedCentroid, smoothedOctaves) the vizzes already read for real audio — so every viz responds automatically. Tap fires a Gaussian transient centered on band at X position, drag X selects dominant band + Y modulates amplitude, hold pads mid bands, key pulses a band selected by char code. On top of the synth, bespoke per-viz `interact(event)` handlers on `VizEntry` add richer behavior for: blobs, grayscott, physarum, rippletank, constellation, marbling, bloom, hive, voronoi, tetris, flowfield, lasers, synthwave, text, highway.
- **`config: Config`** — 7-band freq sensitivities, spikeScale, decayRate, rotationSpeed, masterVolume
- **`audioState: AudioProcessingState`** — smoothedBands, transientValues, deltaValues, spectral centroid, octave data

Events: `stateChange`, `audioReady`, `playbackStart`, `playbackStop`, `modeChange`, `vizModeChange`, `bpmDetected`, `imageChange`, `error`.

### Key audio concepts

- **Transient** — Sudden loudness spike relative to running average. `multiplier > 1.0` means a punch/kick was detected. Exponential decay back to 1.0.
- **Delta** — Smoothed rate of change of amplitude. Distinguishes sustained tones from punchy hits.
- **Auto-gain** — Rolling window of peak values to normalize amplitudes to [0, 1] regardless of track loudness.
- **Bands** — 7 frequency bands: Sub (20-60Hz), Bass (60-250Hz), Low-Mid (250-500Hz), Mid (500-2kHz), Upper-Mid (2-4kHz), Presence (4-6kHz), Brilliance (6-20kHz).

### MIDI mapping

`src/midi/manager.ts` owns all Web MIDI state (module-scoped, no classes):
- `initMidi()` — calls `navigator.requestMIDIAccess()`, attaches `onmidimessage` to all inputs, re-attaches on `onstatechange` (device plug/unplug), loads saved mappings from localStorage.
- CC messages (`0xB0`): if `startMappingMode(configKey)` is active, the next CC resolves the promise and saves the mapping; otherwise the CC value is mapped `0–127 → [slider.min, slider.max]` and dispatched as an `input` event on the slider DOM element.
- Mappings persisted under `localStorage` key `visualizer-midi-mappings`.
- `CONFIG_TO_SLIDER` table maps every `keyof Config` to its slider DOM id.

`src/midi/ui.ts` renders the overlay panel and is initialized by `initUI()` in `controller.ts`. The overlay is injected into `<body>` on first call (not present in static HTML). Styles are injected as a `<style>` tag.

### Adding a new visualization

1. Create `src/visualizations/<name>.ts` exporting `draw<Name>(p: P5Instance, dt: number)` and `reset<Name>()`. Use module-scoped state (no classes). Follow the pattern in `blobs.ts` or `grayscott.ts`.
2. Add `'<name>'` to the `VizMode` union in `src/types/index.ts`.
3. Add config keys to the `Config` interface in `src/types/index.ts` and their defaults to `DEFAULT_CONFIG` in `src/utils/constants.ts`.
4. Add entries to `CONFIG_TO_SLIDER` in `src/midi/manager.ts`.
5. Export `draw<Name>` and `reset<Name>` from `src/visualizations/index.ts`.
6. Add an entry to `src/visualizations/registry.ts` — `draw`, optional `reset`/`dispose`, `key` (keyboard shortcut auto-registered from this), and `label`. Import directly from the viz file (not from `./index`) to avoid circular deps. If the viz uses Three.js, wrap with `lazyViz()`.
7. Add `<option>` in `index.html` viz-selector dropdown.
8. Add slider HTML in a new `<div id="<name>-controls-group" class="hidden">` in `index.html`.
9. Add slider bindings to the `configs` array in `src/ui/sliders.ts` (inside `] as const`).
10. In `src/ui/controller.ts`: add the controls-group ID to `ALL_CONTROL_IDS`, and add the viz to the `VIZ_CONTROLS` record (not a switch/case — it's a record lookup).
11. If the viz requires BPM/beat sync and won't work with mic input, add it to `micHiddenVizModes` in `src/ui/controller.ts`.
12. Add a mobile performance guard using `isMobile` from `src/utils/constants.ts` (reduce pixel buffer resolution, particle count, etc.).
13. Update CLAUDE.md and README.md in the same commit.
14. Add any missing p5.js methods to `src/types/globals.d.ts`.

Note: per-viz keyboard shortcuts have been removed. Users cycle vizzes with the arrow keys (which iterate over the `#viz-selector` `<option>` list) or pick one directly from the sidebar dropdown. The `key` field on registry entries is kept for potential future reintroduction but is not currently registered.

### External libraries

- **p5.js 1.9.0** — CDN-loaded, 2D canvas rendering. Global `p5` constructor, instance passed as `P5Instance`.
- **Tone.js 14.8.49** — CDN-loaded, Web Audio wrapper. `Tone.Player`, `Tone.Gain`, `Tone.FFT`.
- Both have type stubs in `src/types/globals.d.ts` (no `@types` packages).

### iOS app (Capacitor)

`ios/` is a Capacitor-wrapped native iOS project (bundle ID `com.taoong.musicvisualizer`, configured in `capacitor.config.ts`). It loads the same `dist/` production build inside a WKWebView — no visualization or rendering code is native-specific, and rendering performance matches the mobile web version exactly since it's the same web engine. `npm run cap:sync` rebuilds the web app and copies it into `ios/App/App/public`; run this after any web change before testing in Xcode (`npm run cap:open:ios`). Native deps resolve via Swift Package Manager (`ios/App/CapApp-SPM/Package.swift`) — no CocoaPods. `ios/App/App/Info.plist` carries `NSMicrophoneUsageDescription` for mic-mode access; Web MIDI is unsupported in iOS WKWebView, so MIDI mapping is unavailable there. `ios/App/App/public`, `DerivedData/`, and `xcuserdata/` are gitignored via `ios/.gitignore` (Capacitor-generated).

## Maintenance

Keep both this file and `README.md` up to date. After any change that adds/removes/renames files, adds new visualization modes, changes keyboard shortcuts, changes the data flow, modifies the build pipeline, or alters architectural patterns, update the relevant sections of both documents in the same commit.

## Conventions

- Visualizations use module-scoped state (no classes), following the pattern in `circle.ts`.
- p5.js constants accessed via bracket notation: `p['HSB']`, `p['CLOSE']`.
- HSB color mode set per-viz with `(p as any).colorMode(p['HSB'], 360, 100, 100)`, reset to RGB at end.
- Frame-rate independence: all animations multiply by `dt = deltaTime / 16.667`.
- Beat detection pattern: `Math.floor((playbackPosition - beatOffset) / beatIntervalSec)` compared to last index.
